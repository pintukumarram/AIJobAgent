version: "3.9"
services:
  frontend:
    build: ./frontend
    ports:
      - "3000:3000"
  backend:
    build: ./backend
    ports:
      - "5000:5000"
  ai_service:
    build: ./ai_service
    ports:
      - "8000:8000"
